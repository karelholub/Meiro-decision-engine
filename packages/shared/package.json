{
  "name": "@decisioning/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@decisioning/dsl": "workspace:*",
    "@decisioning/engine": "workspace:*",
    "@decisioning/meiro": "workspace:*"
  }
}
